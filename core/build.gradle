apply plugin: "java-library"

repositories {
    jcenter()
}

jar {
    archiveClassifier = "core"
}
 
dependencies {
    implementation group: 'org.jetbrains.kotlin', name: 'kotlin-stdlib', version: kotlin_version

    api project(':api')

    compileOnly group: 'org.slf4j', name: 'slf4j-api', version: libSlf4jVersion
}